# docker-matrix

Provides a Docker container for The Matrix server.


## Usage

Build the container:

````sh
docker build -t jhx0/matrix .
````

Use the container:

````sh
docker run -t -i jhx0/matrix /bin/bash
````


## Contributing

1. Fork it
2. Create your feature branch (`git checkout -b my-new-feature`)
3. Commit your changes (`git commit -am 'Add some feature'`)
4. Push to the branch (`git push origin my-new-feature`)
5. Create new Pull Request


## Acknowledgements

- [Docker](http://docker.com) website
- [CentOS 6](https://registry.hub.docker.com/_/centos/) official Docker repo
